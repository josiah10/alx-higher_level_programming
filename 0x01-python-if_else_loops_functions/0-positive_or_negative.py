#!/usr/bin/python3
import random
number = random.randint(-10, 10)
    if number>0:
        print(f"{i:d}: is positive")
    elif number==0:
        print(f"{i:d}: is zero")
    else:
        print(f"{i:d}: is negative")
